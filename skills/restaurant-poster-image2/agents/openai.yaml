interface:
  display_name: "餐饮宣传图 Image2.0"
  short_description: "发送菜品信息后先询问补充点，再匹配餐饮提示词用 image2.0 出图"
  default_prompt: "Use $restaurant-poster-image2 to turn this dish photo and merchant keywords into an image2.0 restaurant promotional poster, asking once for optional missing details before generation."

policy:
  allow_implicit_invocation: true
