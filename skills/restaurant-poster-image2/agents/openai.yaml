interface:
  display_name: "餐饮宣传图 Image2.0"
  short_description: "按关键词选择原始PDF餐饮提示词，不私自改写后出图"
  default_prompt: "Use $restaurant-poster-image2 to select the closest original PDF-derived Chinese restaurant poster prompt for this dish photo and keywords, replacing only placeholders before image2.0 generation."

policy:
  allow_implicit_invocation: true
